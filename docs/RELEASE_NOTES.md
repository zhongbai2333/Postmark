Postmark · 旅途来信 — 0.1.5（正式版）。

**Minecraft 26.1.2 / NeoForge 26.1.2.75 / Java 25，仅客户端安装。**

使用 `postmark-26.1.2-0.1.5.jar` 替换旧版，保留实例内 `postmark` 数据目录。

- 漫游志展馆详情新增「贴到明信片」：把展馆图片、名称、纸边和胶带一起拿到明信片，只保留展馆纸片，不带角落章、集齐勾或足迹。
- 左键贴上、右键拖动调整大小、滚轮旋转，也可用橡皮擦除。馆名沿用游戏字体。
- 贴纸随卡片保存并参与 PNG 导出，不加入章袋、不计入章数量，也不改变展馆收集进度。

验证：90 项单元测试通过；真实 SMU 客户端贴纸流程和漫游志回归通过。测试在本地合成世界进行，未运行正式活动服务器。
