Postmark · 旅途来信 — 0.1.0-alpha.26（预发布）。

**Minecraft 26.1.2 / NeoForge 26.1.2.75 / Java 25，仅客户端安装。**

使用 `postmark-26.1.2-0.1.0-alpha.26.jar` 替换旧版，保留实例内 `postmark` 数据目录。

修复漫游志集齐圆勾上的碎点与杂色。圆勾先完整合成，再整体旋转，避免逐行绘制时产生细缝。缓存和每个标识一个纹理四边形的优化保留。

alpha.25 的入口拖动记忆、视图恢复、背景延展及左右收藏册底纹继续保留。
